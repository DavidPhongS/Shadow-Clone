Console.Write (" " + i); //same as Console.Write ($(" {i}"));  '+' combine 2 strings tgt

List<datovy tip> nazev = new List<datovy tip>(); //how to insert list

nazev.Contains(neco) //obashuje neco

nazev.Count //pocet prvku u listu

numbers.Remove(2)//remove konkretne prvek

numbers.RemoveAt(0)//remove index (misto)